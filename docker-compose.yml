services:
  dev:
    container_name: spiff-element-units-dev
    build:
      context: .
      dockerfile: dev/Dockerfile
    volumes:
      - ./module:/module

    # TODO: re-integrate the scripts/integration tests
    #  - ./scripts:/app/scripts
    #  - ./spiff_element_units:/app/spiff_element_units
    #  - ./tests:/app/tests